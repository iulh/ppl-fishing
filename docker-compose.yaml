services:
  ppl_fishing:
    container_name: ppl_fishing
    build:
      context: ./
      dockerfile: Dockerfile
    networks:
      - nginx
    command: npm run start
    restart: unless-stopped

networks:
  nginx:
    external: true
    name: nginx
